image_size : 64
nc : 3
batch_size : 32
#4 16 32 64
nz : 100 #complexity measure of GAN, higher values can lead to the GAN being able to capture more diverse
#50 100
epochs: 100
#50/100 for tests, 300/500 for proper.
lr: 0.0001
ngf: 64 #feature map size for generator, higher number = higher quality but slower training
ndf: 64